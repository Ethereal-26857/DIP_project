# 如何在命令行里面打开Jupter notebook？

在命令行（CMD / PowerShell / Terminal）里打开 Jupyter Notebook 一般只需要一条命令。步骤如下：

1️⃣ 打开命令行

Windows 可以用：

CMD

PowerShell

Anaconda Prompt（如果你用的是 Anaconda）

2️⃣ 进入你的项目目录（可选）

例如你的代码在 D:\coding\DIP_project：

cd D:\coding\DIP_project
3️⃣ 启动 Jupyter Notebook

输入：

jupyter notebook

然后回车。

终端会出现类似：

[I 10:23:10 NotebookApp] Serving notebooks from local directory: D:\coding\DIP_project
[I 10:23:10 NotebookApp] Jupyter Notebook 7.x.x is running at:
[I 10:23:10 NotebookApp] http://localhost:8888/

浏览器会自动打开：

http://localhost:8888

这就是 Notebook 页面。

4️⃣ 如果浏览器没自动打开

手动复制终端里的地址，例如：

http://localhost:8888/?token=xxxxx

粘贴到浏览器即可。

⚠️ 常见问题
❌ 找不到 jupyter

如果提示：

'jupyter' 不是内部或外部命令

说明没安装，需要安装：

pip install notebook

或

conda install notebook
❌ 想直接在某个目录打开

可以：

jupyter notebook D:\coding

✅ 给你一个更常用的开发命令（推荐）

如果你在做 Python / 深度学习 / 图像处理，很多人更常用：

jupyter lab

启动 JupyterLab（更像 VSCode 的界面）。

💡 如果你愿意，我可以再教你 3 个超好用的 Jupyter 命令行技巧（比如后台运行、远程服务器运行、SSH断线不断开）。

# 如何激活环境？

如果你的项目环境是用 uv 创建的（项目名 DIP_project），有 两种常见方式进入/激活环境。

方法一（最简单，uv推荐方式）

不用激活环境，直接运行命令：

uv run <command>

例如启动 Jupyter Notebook：

cd D:\coding\DIP_project
uv run jupyter notebook

或者：

uv run python test.py

👉 uv run 会 自动使用项目的虚拟环境。

方法二（真正“激活”环境）

进入项目目录：

cd D:\coding\DIP_project

然后输入：

uv shell

成功后命令行会变成类似：

(DIP_project) PS D:\coding\DIP_project>

这就说明 环境已经激活。

接着就可以直接运行：

python
jupyter notebook
pip install numpy
方法三（手动激活 .venv）

如果 uv 在项目里创建了 .venv 文件夹，也可以直接用 Python 的方式激活：

PowerShell：

.\.venv\Scripts\activate

CMD：

.venv\Scripts\activate.bat

激活后会看到：

(.venv) PS D:\coding\DIP_project>
如何确认环境是否正确

运行：

python -c "import sys; print(sys.executable)"

如果输出类似：

D:\coding\DIP_project\.venv\Scripts\python.exe

说明已经进入 DIP_project 环境。

✅ 最推荐你用的方式：

cd D:\coding\DIP_project
uv run jupyter lab

基本 100%不会出环境问题。

如果你愿意，我可以顺便教你一个 “3秒判断 uv 环境是否正确”的方法（做 Python / DIP 实验非常好用）。

# 为什么会自动创建一个anaconda_projects文件夹啊，我用的是uv管理，且之前已经配好了环境，完全没用anaconda来着
你看到 anaconda_projects 文件夹，其实不是 uv 创建的，而是 Anaconda 自带的 Jupyter 扩展自动生成的。从你的日志里可以看出这一点：

D:\coding\DIP_project\anaconda_projects\db\project_filebrowser.db
aext_assistant
aext_core
aext_toolbox

这些 aext_* 扩展是 Anaconda Notebook 扩展（Anaconda Extensions）。
当 Jupyter Notebook 启动时，如果它是 从 Anaconda 的 Python 启动的，这些扩展就会：

在当前项目目录创建

anaconda_projects/

在里面存数据库

db/project_filebrowser.db

用来保存：

文件浏览器状态

Notebook项目管理

Anaconda Cloud账户信息

所以你看到：

Creating database file...
D:\coding\DIP_project\anaconda_projects\db\project_filebrowser.db

这说明 Jupyter 是从 Anaconda 的 Python 启动的，而不是 uv 环境。

为什么会这样

你的终端提示是：

(base) PS D:\coding\DIP_project>

(base) 说明：

你现在使用的是 Anaconda base 环境。

当你运行：

jupyter notebook

系统执行的是：

C:\Users\26971\anaconda3\Scripts\jupyter.exe

而不是 uv 环境里的 Jupyter。

如何确认

运行：

where jupyter

如果看到：

C:\Users\26971\anaconda3\Scripts\jupyter.exe

那就是 Anaconda 的。

正确的 uv 方式

进入项目目录：

cd D:\coding\DIP_project

然后运行：

uv run jupyter lab

或：

uv run jupyter notebook

这样会使用：

DIP_project\.venv\Scripts\python

而不会加载 Anaconda 插件。

是否需要删除 anaconda_projects

可以删，不影响任何东西：

D:\coding\DIP_project\anaconda_projects

只是一个缓存数据库。

最推荐的配置（避免以后再发生）

在项目里安装 Jupyter：

uv add jupyterlab ipykernel

然后启动：

uv run jupyter lab

这样：

不会再出现 anaconda_projects

不会加载 aext_*

Notebook Kernel 就是你的 uv 环境

💡 一个很关键的小知识（90%的人不知道）

如果你电脑里同时有：

Anaconda

uv

pip venv

系统 很容易默认用 Anaconda 的 jupyter。

很多人以为自己在用虚拟环境，其实没有。

如果你愿意，我可以教你一个 一条命令彻底隔离 Anaconda 和 uv 的方法（以后环境永远不会串）。

# 如何激活文件夹里面的DIP_project环境？
如果你的项目 DIP_project 是用 uv 管理的，并且环境在项目里的 .venv，那么激活方法其实和普通 Python venv 一样。

假设目录结构是：

D:\coding\DIP_project
 ├─ .venv
 ├─ pyproject.toml
 ├─ uv.lock
 └─ ...
方法一：直接激活 .venv（最标准）

在 PowerShell 里：

cd D:\coding\DIP_project
.\.venv\Scripts\Activate.ps1

成功后终端会变成：

(.venv) PS D:\coding\DIP_project>

说明环境已经激活。

之后运行：

python
pip
jupyter notebook

都会使用：

D:\coding\DIP_project\.venv\Scripts\python.exe
方法二：CMD 激活

如果你用的是 CMD：

cd D:\coding\DIP_project
.venv\Scripts\activate.bat
方法三：uv 推荐方式（不用激活）

uv 官方更推荐：

cd D:\coding\DIP_project
uv run python

或：

uv run jupyter lab

uv run 会自动使用 .venv。

如何确认是否真的进入环境

运行：

python -c "import sys; print(sys.executable)"

如果输出：

D:\coding\DIP_project\.venv\Scripts\python.exe

说明环境正确。

如果激活时报 PowerShell 权限错误

有时会出现：

running scripts is disabled

需要运行一次：

Set-ExecutionPolicy RemoteSigned -Scope CurrentUser

然后再激活。

✅ 推荐你以后在这个项目里这样用：

cd D:\coding\DIP_project
.\.venv\Scripts\Activate.ps1
jupyter lab

或者直接：

uv run jupyter lab

如果你愿意，我可以再教你一个 最干净的配置：
让 Visual Studio Code + Jupyter 永远自动使用 .venv，完全不会再误用 Anaconda。

# --------------------------------------------------------------------------- ModuleNotFoundError Traceback (most recent call last) Cell In[1], line 1 ----> 1 import cv2 2 import numpy as np 3 import matplotlib.pyplot as plt ModuleNotFoundError: No module named 'cv2'
明白了，你现在报的错误是：

ModuleNotFoundError: No module named 'cv2'

这说明你当前 Notebook 的 DIP_project .venv 环境里还没有安装 OpenCV（cv2 模块）。

解决方法
1️⃣ 确认你在 .venv 环境

在 Notebook 里运行：

import sys
print(sys.executable)

输出应该是：

D:\coding\DIP_project\.venv\Scripts\python.exe

如果是，说明你正在用 正确的虚拟环境。

2️⃣ 安装 OpenCV

激活 .venv（Git Bash）：

cd D:/coding/DIP_project
source .venv/Scripts/activate

然后安装 OpenCV：

pip install opencv-python

如果你还需要额外的功能（比如 opencv-contrib 模块），可以：

pip install opencv-contrib-python
3️⃣ 安装常用科研包（可选，一次搞定）

在 .venv 里：

pip install numpy matplotlib scipy opencv-python pillow

这样以后做图像处理和科学计算就不会再报 ModuleNotFoundError。

4️⃣ Notebook 再运行
import cv2
import numpy as np
import matplotlib.pyplot as plt

✅ 不会报错，就可以运行前面给你的 Zooming & Shrinking Images 代码了。

我可以帮你写一个 完整的 DIP_project 环境初始化命令，
一次性安装 numpy, matplotlib, scipy, cv2, pillow，并注册 Notebook Kernel，确保以后 Notebook 直接可用，不再碰到模块找不到的问题。

你希望我帮你写吗？

# 26971@▒▒▒▒ MINGW64 /d/coding/DIP_project (LAB1) $ ./.venv/Scripts/python -m pip install opencv-python D:\coding\DIP_project\.venv\Scripts\python.exe: No module named pip (DIP_project) 26971@▒▒▒▒ MINGW64 /d/coding/DIP_project (LAB1) $
明白了，这个报错：

No module named pip

说明你的 .venv 环境里还没有 pip，所以即使激活了 .venv，也没法安装任何包。

这种情况在 Windows 上用 uv 创建 .venv 时偶尔会发生。下面是完整解决方法。

1️⃣ 确认 Python 在 .venv 里
./.venv/Scripts/python --version

输出应该是 DIP_project 的 Python 版本，例如 Python 3.12.x。

2️⃣ 给 .venv 安装 pip

使用官方 ensurepip：

./.venv/Scripts/python -m ensurepip --upgrade

这会在 .venv 里安装 pip

成功后，你可以用 .venv 的 pip 安装包

测试：

./.venv/Scripts/python -m pip --version

输出应该类似：

pip 26.0.1 from D:\coding\DIP_project\.venv\Lib\site-packages\pip (python 3.12)
3️⃣ 安装 OpenCV（及常用包）
./.venv/Scripts/python -m pip install --upgrade pip
./.venv/Scripts/python -m pip install numpy matplotlib scipy opencv-python pillow

这样就能把包都装到 DIP_project .venv 里。

4️⃣ 注册 Notebook Kernel

确保 Jupyter Notebook 用这个 .venv：

./.venv/Scripts/python -m ipykernel install --user --name DIP_project --display-name "DIP_project"

然后打开 Notebook，选择 Kernel DIP_project，就能正常导入所有包了。

💡 总结

目前你的问题是：

.venv 里没有 pip → 无法安装任何包

系统 Python 里有 opencv，但 Notebook 用的是 .venv → 找不到 cv2

解决步骤就是：

安装 pip → 安装包 → 注册 Kernel

如果你需要，我可以帮你写一条 完整命令，一条执行就能完成：

激活 .venv

安装 pip

安装 numpy、matplotlib、scipy、opencv、pillow

注册 Notebook Kernel

让 DIP_project Notebook 直接可用。

你希望我帮你写吗？

# --------------------------------------------------------------------------- ValueError Traceback (most recent call last) Cell In[7], line 13 11 img = cv2.imread(image_path, cv2.IMREAD_GRAYSCALE) 12 if img is None: ---> 13 raise ValueError("图像路径不正确或文件不存在") 15 # 提取一行灰度值 16 original_line = img[line_row, :] ValueError: 图像路径不正确或文件不存在
明白了，这次报错：

ValueError: 图像路径不正确或文件不存在

说明 Python 找不到你指定的图像文件。常见原因有：

路径写错

使用了反斜杠 \，在 Python 字符串里可能被当作转义

文件确实不在你写的目录里

解决方法
1️⃣ 检查路径

假设你的文件在：

D:\课程\专业课\生物医学图像处理\lab1\lab1.mat

Python 里建议写成 原始字符串 或用 正斜杠 /：

image_path = r"D:\课程\专业课\生物医学图像处理\lab1\lab1.mat"
# 或者
image_path = "D:/课程/专业课/生物医学图像处理/lab1/lab1.mat"

注意：r"" 前缀表示原始字符串，不会把 \ 当作转义。

2️⃣ 如果是 .png 或 .jpg，注意文件名和后缀

例如：

image_path = r"D:\课程\专业课\生物医学图像处理\lab1\lab1.png"

你的代码里可能写的是 lab1.mat，但 cv2.imread 不能直接读取 .mat 文件，只能读取图片文件。

如果是 .mat 文件（MATLAB 格式）：

import scipy.io as sio
mat = sio.loadmat(image_path)
img = mat['image_variable_name']  # 替换成 .mat 里保存的变量名
3️⃣ 测试文件是否存在

在 Notebook 里先测试：

import os

image_path = r"D:\课程\专业课\生物医学图像处理\lab1\lab1.png"
print(os.path.exists(image_path))

输出 True → 文件存在，可以用

输出 False → 路径写错或者文件不在这个位置

如果你告诉我你要用的是 .png/.jpg 图像 还是 .mat 文件，我可以帮你改写 完整 Zooming & Shrinking 图像代码，直接支持你的文件格式。

你这个是 .mat 文件吗？

# --------------------------------------------------------------------------- ValueError Traceback (most recent call last) Cell In[11], line 19 16 else: 17 # 打印变量名，确认里面的图像变量 18 print(mat.keys()) ---> 19 raise ValueError("请确认 .mat 文件里图像变量名") 21 # 如果是二维图像，确保类型为 uint8 22 img = np.array(img, dtype=np.uint8) ValueError: 请确认 .mat 文件里图像变量名
明白了，这次报错说明你的 .mat 文件里 没有名为 'img' 的变量。

我们需要先查看 .mat 文件里到底有哪些变量，然后才能提取灰度图像。

1️⃣ 打印变量名
import scipy.io as sio

mat_path = r"D:\课程\专业课\生物医学图像处理\lab1\lab1.mat"

mat = sio.loadmat(mat_path)
print(mat.keys())

输出可能类似：

dict_keys(['__header__', '__version__', '__globals__', 'lab1_image'])

其中 __header__, __version__, __globals__ 是系统信息，真正的图像变量通常是其他名字（比如 'lab1_image'）。

2️⃣ 使用正确的变量名

假设打印结果显示图像变量名是 'lab1_image'，那么：

img = mat['lab1_image']

然后继续处理：

img = np.array(img, dtype=np.uint8)

💡 小技巧：

如果你不确定哪一个是图像，可以查看形状：

for key in mat.keys():
    if not key.startswith('__'):
        print(key, mat[key].shape, mat[key].dtype)

一般二维数组 (height, width) 就是灰度图

三维数组 (height, width, 3) 就是彩色图

我可以帮你把 之前的 Zooming & Shrinking 代码改成自动检测 .mat 文件里灰度图变量，不管名字是什么都能用。

你希望我帮你改吗？

# 为什么感觉不太对？有一块最亮的白色区域没有变成红色，其他白色区域变成红色了
哦，这个现象其实是 叠加逻辑的问题，和你看到的“最亮白色区域”有关。我们来分析一下原因。

1️⃣ 你现在的逻辑
mask = img > threshold
color_img = np.stack([img, img, img], axis=-1)
color_img[mask, 0] = 255  # 红色通道
color_img[mask, 1] = img[mask]
color_img[mask, 2] = img[mask]

mask 为 True 的地方：

红色通道设置为 255

绿色和蓝色通道仍保持原灰度值

问题出现在 原灰度图已经接近 255 的区域：

假设原灰度值 img[y,x] = 255（最亮的白）

mask 为 True

红色通道设置 255

绿色通道 img[mask] = 255

蓝色通道 img[mask] = 255

结果就是 (255, 255, 255) → 白色，而不是红色！

✅ 所以最亮的白色区域不会“变红”，因为它已经是全白了。

2️⃣ 解决方法

要确保 mask 区域显示为红色，可以把 绿色和蓝色通道设置为更低的值，比如 0。这样不管原图多白，都会变红：

#红色叠加（强制绿色和蓝色为0）
color_img[mask, 0] = 255  # 红色通道
color_img[mask, 1] = 0    # 绿色通道
color_img[mask, 2] = 0    # 蓝色通道

这样 (255,0,0) → 明亮红色

原本白色或浅灰色的区域都会被 mask 高亮为红色

3️⃣ 完整修改后的 Color Overlay 代码
import scipy.io as sio
import numpy as np
import matplotlib.pyplot as plt

#参数
mat_path = r"D:\课程\专业课\生物医学图像处理\lab1\lab1.mat"
threshold = 128

#读取 .mat 文件
mat = sio.loadmat(mat_path)
if 'data' in mat:
    img = mat['data']
else:
    raise ValueError("请确认 .mat 文件里图像变量名")

img = np.array(img, dtype=np.uint8)

#生成 mask
mask = img > threshold

#转为 RGB
color_img = np.stack([img, img, img], axis=-1)

#红色叠加（确保亮区域也变红）
color_img[mask, 0] = 255
color_img[mask, 1] = 0
color_img[mask, 2] = 0

#显示结果
plt.figure(figsize=(6, 6))
plt.imshow(color_img)
plt.title(f"Red Overlay: I > {threshold}")
plt.axis('off')
plt.show()

✅ 效果：

所有大于阈值的区域都会变成红色

不再有原本最亮的白色区域不显示红色的问题

如果你愿意，我可以再帮你写一个 更“自然”的红色叠加版本，不是把绿蓝通道全置零，而是 保留原亮度的同时红色增强，这样不会完全覆盖灰度感，看起来更像科学叠加图。

